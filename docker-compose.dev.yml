services:
  api:
    ports:
      - "127.0.0.1:${API_PORT:-3000}:3000"

  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"

  redis:
    ports:
      - "127.0.0.1:${REDIS_PORT:-6379}:6379"
